# 设计语言导入（填写后整段发给 Agent）

> 母版路径：`_shared/templates/design-languages/design-language-intake.template.md`  
> Agent 按 [`design-language-import.spec.md`](../../specs/ui-design/design-language-import.spec.md) 分析并创建 `_shared/design-languages/<id>/`。

---

来源（可选，URL 或站点名）：

建议 id（可选，英文 kebab-case，如 `apple-hig`、`ant-design`）：

显示名称（可选，如 Apple Human Interface Guidelines）：

---

## 原始 prompt（必填）

将外部获取的设计语言 / design token **全文**粘贴在下方（可含 `<role>`、`<design-system>` 或纯 Markdown）：

```text
（在此粘贴全文）
```
