{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "ix-gui 默认权限：核心 + PTY + Dialog 插件",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "pty:default",
    "dialog:default"
  ]
}
